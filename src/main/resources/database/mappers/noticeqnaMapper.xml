<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.winter.app.board.notice.NoticeQnaDAO">
  
  
  <select id="getList" resultType="NoticeDTO" parameterType="Pager">
	
	SELECT * FROM
		 (SELECT ROWNUM N, R.* FROM	
		(SELECT * FROM NOTICEQNA
		ORDER BY BOARDNUM DESC) R)
	WHERE N BETWEEN #{start_Num} AND #{last_Num}
	
  </select>
  
  <insert id="setAdd" parameterType="BoardDTO">
    <selectKey keyProperty="boardNum" resultType="Long" order="BEFORE">
    SELECT BOARD_SEQ.NEXTVAL DUAL
    </selectKey>
  	INSERT INTO NOTICEQNA(BOARDNUM,BOARDTITLE,BOARDWRITER,
  	BOARDCONTENTS,BOARDDATE,BOARDHIT,BOARDREF,BOARDSTEP,BOARDDEPTH)
  	VALUES(#{boardNum},#{boardTitle}, #{boardWriter}, 
  	#{boardContents},SYSDATE,0,#{boardNum},0,0)
  </insert>
  </mapper>